import { ValidTelsEqDirective } from './valid-tels-eq.directive';

describe('ValidTelsEqDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidTelsEqDirective();
    expect(directive).toBeTruthy();
  });
});
