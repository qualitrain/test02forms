import { ValidContLogicDirective } from './valid-cont-logic.directive';

describe('ValidContLogicDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidContLogicDirective();
    expect(directive).toBeTruthy();
  });
});
